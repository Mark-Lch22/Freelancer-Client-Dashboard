# Copy this file to .env.prod and fill in real values.
# .env.prod is git-ignored and must NEVER be committed.
#
# Usage:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up -d

# ---------------------------------------------------------------------------
# Docker Hub
# ---------------------------------------------------------------------------
# Your Docker Hub username or organisation (e.g. "acmecorp")
DOCKERHUB_USERNAME=yourusername

# Image tag to deploy (default: latest).
# Pin to a specific SHA for reproducible deploys, e.g. sha-abc1234
TAG=latest

# ---------------------------------------------------------------------------
# Host ports
# ---------------------------------------------------------------------------
# Port on the host machine that forwards to the backend container (3000)
BACKEND_PORT=3000

# Port on the host machine that forwards to the nginx frontend container (80)
FRONTEND_PORT=80

# ---------------------------------------------------------------------------
# Supabase — all values found in your Supabase project under Settings > API
# ---------------------------------------------------------------------------
SUPABASE_URL=https://your-project-ref.supabase.co

# anon/public key — used by NestJS for non-privileged queries
SUPABASE_ANON_KEY=your-anon-public-key

# service_role key — bypasses Row Level Security; NEVER expose to the browser
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT secret — used to verify Supabase tokens server-side (Settings > API > JWT Settings)
SUPABASE_JWT_SECRET=your-jwt-secret
